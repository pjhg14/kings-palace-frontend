export const initialState = {
    cable: "",
    subscription: "",
    cancelSub: () => {}
}

export function subscriptionReducer(state, action) {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}